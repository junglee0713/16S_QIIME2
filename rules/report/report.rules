rule all_report:
    input:
        TARGET_REPORT

rule generate_report:
    input:
        mapping_fp = MAPPING_FP
    output:
        CORE_METRIC_DIR + "/" + config["report"]["report"] + "/Basic_Bioinformatics_Report.html"
    script:
        str(config["report"]["rmd"]) 

