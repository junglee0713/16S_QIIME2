rule all_report:
    input:
        TARGET_REPORT

rule generate_report:
    input:
        mapping_fp = MAPPING_FP,

        
    output:
        DADA2_denoise_dir + "/" + config["report"]["report"] + "/Basic_Bioinformatics_Report.html"
    script:

